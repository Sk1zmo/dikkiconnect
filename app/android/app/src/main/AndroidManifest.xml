<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- "Use my location" on the pickup address, and Recentre on the tracking
         map, both call navigator.geolocation. Android grants the WebView
         nothing without these, and the failure is silent — the promise simply
         never resolves. Coarse is listed too so a user who declines precise
         location still gets a usable map centre. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Vector tiles are cached to disk; without this the map re-downloads the
         whole viewport on every cold start. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera: the parcel QR scanner and the photo evidence captured at every
         handover. `required="false"` so the app still installs on a device
         without one — a hub terminal, say — rather than being filtered out of
         the Play Store for it. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- Photos. Android 13 split READ_EXTERNAL_STORAGE into per-type
         permissions, so both spellings are needed: the new one for 13+, the
         old one capped below it, or the app can read nothing on one half of
         the devices in the field. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Keeps the screen awake during turn-by-turn navigation. A driver who has
         to poke the phone at a junction is a driver not watching the road. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

</manifest>
